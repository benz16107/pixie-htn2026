# Rubric: Atlas design + plan
Score 1-5 each with one line of evidence (file + section); recommend a base; list 1-4 grafts.
1. Federato fit: hits MVP/Strong/Exceptional/Bonus explicitly (dynamic queries from schema, traceable adaptive reasoning, contradictions, enrichment that visibly changes ranking, portfolio context), correct handling of the real data (bound vs open queue, missing premium, property-only appetite, hq-location path, ground-truth declines).
2. Multi-agent quality: genuine decomposition/communication/coordination (not a pipeline in disguise), visible per-agent traces, deterministic scoring kept in code, reusable design (Huawei openJiuwen criteria).
3. Intact slide-over: the Expo app truly reuses the engine; meets Intact's 5 criteria incl. accessibility and README; fairness guardrails; the "View as underwriter" link is real.
4. Track coverage done honestly: every stacked track (Rox, OpenAI+Codex, Linq, Sentry, Composio, Elastic, Expo, Gemini, ElevenLabs, Huawei) is load-bearing with a concrete component and a 60-s booth proof; sane priority and cut order.
5. Solo buildability by 08:00 with 2-3 coding agents: realistic hour plan, parallel lanes with clean seams, early demoable milestones, caching so the demo never depends on live APIs, a precomputed run, fallbacks.
6. Demo: a 5-minute script that lands the wow (backtest, plain-English query, agent lanes, map, phone, live iMessage/email) and 60-s booth variants.
